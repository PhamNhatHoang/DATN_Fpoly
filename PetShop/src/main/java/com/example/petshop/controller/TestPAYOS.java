package com.example.petshop.controller;

import vn.payos.PayOS;
import vn.payos.type.CheckoutResponseData;
import vn.payos.type.ItemData;
import vn.payos.type.PaymentData;

public class TestPAYOS {
//    PayOS payOS = new PayOS("API_KEY", "ajdha|", "API_SECRET");
//    ItemData itemData = ItemData.builder().name("Mỳ tôm Hảo Hảo ly").quantity(1).price(2000).build();
//    PaymentData paymentData = PaymentData.builder().orderCode("ORDER_CODE").totalAmount(2000).build();
}
